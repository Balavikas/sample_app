<% provide(:title, 'user') %>
<div class="row" >
    <aside class="col-md-4">
        <section class="user_info">
            <h1>
                <%= avatar(@user) %>
                <%= @user.name.titleize %>
            </h1>
        </section>
    </aside>
</div>